# ===== CHOOSE ONE: OpenAI OR AWS Bedrock =====

# OpenAI Configuration (if using OpenAI models)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-3.5-turbo

# Popular OpenAI models you can use:
# OPENAI_MODEL=gpt-3.5-turbo (default, faster and cheaper)
# OPENAI_MODEL=gpt-4 (more capable but slower and more expensive)
# OPENAI_MODEL=gpt-4-turbo (latest GPT-4 variant)

# ===== OR =====

# AWS Configuration for Bedrock Access
# Get these from AWS IAM Console > Users > [Your User] > Security credentials > Create access key

AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here
AWS_DEFAULT_REGION=us-east-1

# Optional: If you prefer using AWS profiles instead of access keys
# AWS_PROFILE=your_profile_name

# Bedrock Model Configuration
# Default model (you can change this to any Bedrock model you have access to)
BEDROCK_MODEL_ID=amazon.titan-text-express-v1

# Other popular Bedrock models you can use:
# BEDROCK_MODEL_ID=anthropic.claude-3-sonnet-20240229-v1:0
# BEDROCK_MODEL_ID=anthropic.claude-3-haiku-20240307-v1:0
# BEDROCK_MODEL_ID=cohere.command-text-v14 